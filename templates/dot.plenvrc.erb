#
# This is a shell fragment that initializes plenv, if it
# has not been inited yet. Managed by puppet - DO NOT EDIT
#
if ! echo $PATH | grep -q plenv; then
  export PATH="<%= @root_path %>/bin:$PATH"
  eval "$(plenv init -)"
fi
